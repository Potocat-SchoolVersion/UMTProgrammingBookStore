import os
#import cv2 as cv
import time


def face_comparision(target_image_name, threshold=0.4):
    is_same = False
    distance = -1.0



    return True, 0


# is_same, dist = face_comparision("601116181428")
# print(is_same, dist)
